# Public Data Strategy

Projects 2–5 use the following pattern:

```text
try public molecular dataset
→ if network fails, use embedded demo data
→ standardise columns
→ clean SMILES and target values
→ randomly sample up to 500 molecules
→ run downstream model workflow
```

The cap keeps the notebooks suitable for ordinary laptops and GitHub users who may not have large compute resources.
